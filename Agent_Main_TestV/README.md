Agent_Main_TestV: the original single-agent build - one agent handling extract > parse > report via the generative orchestrator. Abandoned because the orchestrator carried session context across runs and improvised (double outputs, re-greetings, drift between fresh and continued sessions), making it non-repeatable. Replaced by the deterministic Extractor > Parser > Reporter chain. Kept for reference - not part of the live pipeline.
